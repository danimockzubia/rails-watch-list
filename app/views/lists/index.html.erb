<%# <div class='container'>
  <div class="cover mb-4 d-flex align-items-center justify-content-center">
    <div class="text-center">
      <h1>Save any kind of movies</h1>
      <h3 class="typed-js"></h3>
    </div>
  </div>
  <div class="container py-5">
    <div class="d-flex align-items-center justify-content-between mb-3">
      <h3 class="mb-0">My Lists</h3>
      <%= link_to 'Create a movie list', new_list_path, class: 'btn' %>
  <%# </div>

  <% @lists.each do |list| %>
      <%# <div class='cards'>
        </li>
          <% if list.photo.attached?%>
            <%# <div class="card-category" style="background-image: url('<%= cl_image_path list.photo.key, height: 300, width: 400 %>
              <%# <%= link_to list.name, list_path(list), class: "nav-link" %>
            <%# <% else%>
          <%# <% end %>
            <%# </div>
        </li>
      </div>
    <% end %>
<%# </div> %>


<body>

  <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <a class="navbar-brand" href="/">All Lists</a>
  </nav>
  <div class='container mt-3'>
      <div class='row'>
        <div class='col-12'>
          <h3>Lists(<%= @lists.size %>)</h3>
        </div>
      </div>
      <div class='row'>
        <% @lists.each do |list| %>
          <div class="col-4">
            <div class='cards'>
              <div class='content-card'>
                <h3><%= link_to list.name, list_path(list), class: "nav-link" %></h3>
                <div class="card-category" style="background-image: url('<%= cl_image_path list.photo.key, height: 300, width: 400 %>')"></div>
              </div>
            </div>
          </div>
        <% end %>
      </div>
          <br>
      <div class='button-card'>
        <%=link_to "Add a List", new_list_path, class: 'btn' %>
      </div>
  </div>
</body>
